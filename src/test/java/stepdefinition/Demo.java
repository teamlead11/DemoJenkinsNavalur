package stepdefinition;

import java.util.List;

import org.junit.Assert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import cucumber.api.java.en.Given;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;
import io.cucumber.datatable.DataTable;

public class Demo {
	//static WebDriver driver;

	/*
	 * @Given("The user is in add customer page") public void
	 * the_user_is_in_add_customer_page() {
	 * System.setProperty("webdriver.chrome.driver",
	 * "D:\\chromedriver\\chromedriver.exe"); driver = new ChromeDriver();
	 * driver.get("http://demo.guru99.com/telecom/addcustomer.php"); }
	 * 
	 * @When("The user enters incorrect value in the first name field") public void
	 * the_user_enters_incorrect_value_in_the_first_name_field(DataTable fname) {
	 * List<String> fnameValue = fname.asList(String.class);
	 * driver.findElement(By.id("fname")).sendKeys(fnameValue.get(0));
	 * 
	 * }
	 * 
	 * @Then("The user should see the error message") public void
	 * the_user_should_see_the_error_message(DataTable dataTable) { List<String>
	 * errormessage = dataTable.asList(String.class);
	 * Assert.assertEquals(errormessage.get(0),
	 * driver.findElement(By.xpath("//label[@id='message']")).getText()); }
	 */

}
